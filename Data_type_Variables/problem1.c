#include <stdio.h>

int a = 10, b = 20, c = 30, sum; // Declare and initialize variables globally

int main()
{
    sum = a + b + c;        // Calculate sum
    printf("Sum: %d", sum); // Print the sum
    return 0;               // Return 0 to indicate successful completion
}
